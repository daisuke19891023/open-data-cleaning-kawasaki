# Copy this file to `.env` and edit.
# 機密値はこのファイルに直接書かず、`.env` でのみ指定してください。

MP_APP_NAME="MyProject"
MP_DEBUG=false
MP_DATABASE_URL="postgresql://YOUR_APP_USER:YOUR_APP_PASSWORD@YOUR_DB_HOST:5432/YOUR_APP_DB"

# PostgreSQL connection string used by ETL pipelines
DB_DSN="postgresql+psycopg://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_DB_HOST:5432/YOUR_DB_NAME"
# Optional: override the path to DB config yaml
DB_CONFIG_PATH="configs/db.yml"
